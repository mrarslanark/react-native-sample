import TYPES from '../actions/types';

const initialState = {
  entities: [],
};
const reducer = (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};
export default reducer;
